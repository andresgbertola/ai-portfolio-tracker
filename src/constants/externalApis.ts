// src/constants/api.ts
const API_BASE_URL = "api";

const ENDPOINTS = {
  ASSETS: "/mock_assets",
};

export const API_URLS = {
  ASSETS: `${API_BASE_URL}${ENDPOINTS.ASSETS}`,
};

// src/constants/api.ts
export const API_LOCAL_BASE_URL = "eapi";